export default function Loading() {
  return (
    <main
      aria-busy="true"
      className="grid min-h-dvh place-items-center px-5 text-center"
    >
      <p className="font-mono text-sm uppercase tracking-[0.2em] text-cyan">
        Loading mission console
      </p>
    </main>
  );
}
